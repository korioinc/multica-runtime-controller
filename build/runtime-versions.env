# Compiler exists only in the core build stage. Operator tools belong to the Helm environment profile.
GO_VERSION=1.26.1

# Checksum-verified, unmodified official daemon artifact.
MULTICA_CLI_VERSION=0.4.41
